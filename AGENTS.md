# AI Constitution

このファイルは、このリポジトリにおける AI 向け運用ルールの正本です。
Codex、Claude、Copilot など複数の AI を使う場合でも、共通ルールは常にこの `AGENTS.md` を優先します。

## Purpose

- 全 AI に共通する恒久ルールを定義する
- ルールの優先順位と責務分担を定義する
- 個別ツール向け文書が従うべき基準を定義する

## Priority

1. この `AGENTS.md`
2. 各ツール向けの補助ファイル
3. 個別のプロンプトや会話内の追加指示

補助ファイルや追加指示が `AGENTS.md` と矛盾する場合は、`AGENTS.md` を正とします。

## Global Rules

- 既存の設計意図を尊重し、不要な変更を広げない
- 実際に変更していない内容を説明、提案、コミットメッセージに含めない
- 変更理由や判断根拠は、必要な範囲で簡潔に示す
- ツール固有の制約があっても、プロジェクト共通ルールを勝手に上書きしない
- フォールバックは本当に必要な場合に限る。根本原因を隠すためのフォールバックを追加しない

## Engineering Principles

- SSOT: ルール、仕様、用語、実装方針を複数箇所に重複記載しない。正本を決め、他は参照でつなぐ
- DRY: 同じ意図のコード、文言、設定、説明を繰り返さない
- KISS: まず最小で単純な構造を選ぶ。不要な抽象化や過剰な分岐を増やさない
- YAGNI: 今必要な要件を超える拡張点、設定、汎用化を先回りで作らない
- PIE: Program Intently and Expressively。名前、構造、分割で意図が伝わるように書く。Why がコードだけで伝わらない場合のみ、短いコメントで補う
- SOLID: オブジェクト指向に限らず、責務分離、依存方向、変更影響の局所化を意識して設計する
- Fallback Minimalism: フォールバックは障害耐性や互換性のために必要な場合だけ使う。問題の隠蔽、暫定延命、原因調査の妨げになるフォールバックは避ける

## Commit Policy

コミットメッセージ生成時は次を厳守:

1. `docs/commit-convention.md` の規約に従う
2. 日本語の Conventional Commits 形式で出力
3. 変更差分にない内容は書かない
4. 出力はコミットメッセージ本文のみ

AI がコミットする前には、変更内容に応じたレビューを必ず実施する。

- AI 向け文書の変更は `skills/ai-doc-review/` の観点でレビューする
- それ以外の変更は `skills/review-workflow/` の観点でレビューする
- レビューで見つかった問題を未解決のままコミットしない

詳細な規約と生成用プロンプトは次を参照:

- `docs/commit-convention.md`
- `docs/commit-prompt.md`

## Delegated Docs

このファイルには、全 AI に共通する原則だけを書く。
次のような詳細は、個別ファイルまたは補助文書へ委譲する。

- ツール固有の読み込み仕様や制約
- 一時的な作業メモ
- 特定機能や特定ディレクトリだけに適用する局所ルール
- 長い手順書、テンプレート、運用例

## Per-Tool Files

- `CLAUDE.md` は Claude 向けの補助ファイルとして使う
- GitHub Copilot 向けの補助ファイルは `.github/copilot-instructions.md` に置く
- 補助ファイルには共通ルールを重複記載せず、この `AGENTS.md` を参照させる

## Shared Skills

- 共用スキルは `skills/` 配下に置く
- スキルは AI やツール固有の使い方ではなく、再利用可能な作業フローと判断基準を定義する
